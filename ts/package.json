{
  "name": "ts",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "live": "live-server ./bundle --watch --open",
    "compiler": "tsc --project tsconfig.json --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.1",
    "react": "^16.13.1",
    "typescript": "^3.8.3"
  }
}
